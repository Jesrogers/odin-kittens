<h1>Homepage for <%= @kitten.name %></h1>

<%= link_to "Edit", edit_kitten_path(@kitten) %>
<%= link_to "Delete", kitten_path(@kitten), method: :delete, data: { confirm: "Are you sure you want to delete #{@kitten.name}?"} %>